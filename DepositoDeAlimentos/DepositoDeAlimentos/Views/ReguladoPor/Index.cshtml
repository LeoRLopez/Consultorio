@model IEnumerable<DepositoDeAlimentos.Models.ReguladoPor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MarcoRegulatorio.Duracion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Presentado.IdPresentado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MarcoRegulatorio.Duracion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Presentado.IdPresentado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdReguladoPor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdReguladoPor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdReguladoPor })
        </td>
    </tr>
}

</table>
